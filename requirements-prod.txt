# Production dependencies for scaled deployment
# Install: pip install -r requirements-prod.txt

# Process management for multiple workers
gunicorn>=21.0.0
uvloop>=0.19.0  # Faster event loop (macOS/Linux)
httptools>=0.6.0  # Faster HTTP parsing

# Include base requirements
-r requirements.txt
